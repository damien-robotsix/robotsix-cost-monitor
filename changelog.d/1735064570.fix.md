All config fields in `AuthConfig` and `Settings` now carry Pydantic `Field(description=...)` annotations, promoting existing code comments. The generated `config/config.schema.json` includes field-level `description` keys so the deploy UI shows inline help text for every config knob.