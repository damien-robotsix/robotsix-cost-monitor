Adopt shared AppShell navigation shell from `@robotsix/ui` v0.1.40 across dashboard and settings pages. Bump the npm git pin from `#v0.1.30` to `#v0.1.40`, mount `mountAppShell` (imported from vendored `vanilla.js`) as persistent top-of-page chrome with ordered nav items (Dashboard, Settings) and active-link highlighting. Replace the bespoke `<header>` on both pages with `#appshell-root` container. Rename `.controls` CSS class to `.controls-bar` to avoid conflict with AppShell internals; remove orphaned `.nav-link` styles. Re-vendor `dist/style.css` and `dist/vanilla.js` from the updated dependency.
