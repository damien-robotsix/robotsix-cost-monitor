name: Bump dependencies

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  bump:
    uses: damien-robotsix/robotsix-mill/.github/workflows/deps-bump.yml@a063c729739539138db80ce72c82a35e38eb76f9  # maintainers: update SHA and version together
    secrets: inherit
