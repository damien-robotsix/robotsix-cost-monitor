name: CodeQL Analysis
on:
  push:
    branches: [main]
  pull_request:
    paths:
      - '**/*.py'
      - '**/*.js'
      - '**/*.ts'
permissions:
  # Minimal default: only need to read repository contents
  contents: read
jobs:
  codeql:
    name: CodeQL analysis
    uses: damien-robotsix/robotsix-github-workflows/.github/workflows/codeql.yml@86dae89b637dad27ddba0aaa5f9430e083288365  # main
    permissions:
      # security-events: write needed to upload CodeQL SARIF results
      contents: read
      security-events: write  # upload CodeQL SARIF results to the Security tab
    with:
      languages: python,javascript-typescript
      config-file: .github/codeql/codeql-config.yml
