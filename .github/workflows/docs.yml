name: Docs

on:
  push:
    branches: [main]

jobs:
  docs:
    timeout-minutes: 10
    permissions:
      contents: write
    uses: damien-robotsix/robotsix-github-workflows/.github/workflows/python-docs.yml@5cd8c1facb02299da618979e7a55f1953f9c075a  # maintainers: update SHA and version together
