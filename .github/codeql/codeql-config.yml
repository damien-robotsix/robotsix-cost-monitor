# CodeQL configuration for robotsix-cost-monitor.
#
# The `py/path-injection` rule flags `data_dir` → path construction in
# _utils.py.  `data_dir` is a configuration-controlled, trusted directory —
# the same safe pattern that is already accepted on main.  We exclude this
# specific file rather than disabling the rule project-wide.
paths-ignore:
  - src/robotsix_cost_monitor/_utils.py
