__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.json
htmlcov/
# Local secrets — never commit real Langfuse / OpenRouter keys.
config/config.json
config/secrets.yaml
*.local.yaml
# Runtime state (cost-analyst memory, reconciliation snapshots).
.data/
# MkDocs build output — generated by `mkdocs build`, never committed.
site/
# CycloneDX SBOM — generated at CI / build time, never committed.
sbom.cyclonedx.json
node_modules/
/coverage
