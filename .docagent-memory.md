{"properties": {"user_facing": true, "summary": "This is a user-facing API behavior change: unknown `?project=<slug>` query params now return HTTP 404 with `{\"error\":{\"code\":\"PROJECT_NOT_FOUND\",...}}` on all project-scoped endpoints instead of 200-with-empty. The documentation was already updated in the commit via a thorough Keep-a-Changelog entry in `CHANGELOG.md` (unreleased section) listing the affected endpoints and the root cause (the previously-dead `ProjectNotFoundError` now raised from `CostService._projects()`). I verified README.md and docs/ were silent on the old 200-empty behavior, so there was no stale prose to correct, and no per-endpoint error handling is documented anywhere else. The other user-facing doc surface for the API contract — the `_CHAT_SKILL` string in `src/robotsix_cost_monitor/routes.py` — would benefit from a note that a non-matching `<scope>` returns 404 with PROJECT_NOT_FOUND (distinct from a valid-but-empty project, and that `?project=all` stays 200), but that file is under the blocked `src/` tree so this agent could not edit it. Recommend the implementing change add that note to `_CHAT_SKILL` so the chat-skill contract doc is consistent with the new behavior. Otherwise no further doc changes were needed.", "updated_memory": "Repo doc layout recorded: README.md (API table, all endpoints), AGENT.md (testing/config), ARCHITECTURE.md, CONTRIBUTING.md, CHANGELOG.md (Keep-a-Changelog, unreleased at top), docs/ MkDocs site (index.md, api.md auto-generated, reference/cli.md, reference/configuration.md, modules.yaml taxonomy), changelog.d/ fragments. API error contract: consistent JSON envelope `{\"error\":{\"code\":...,\"detail\":...}}`; codes include PROJECT_NOT_FOUND (404), VALIDATION_ERROR (422), HTTP_ERROR, INTERNAL_ERROR (500). New documented behavior: unknown ?project= slug returns 404 PROJECT_NOT_FOUND on all project-scoped endpoints (summary/by-agent/by-model/backend-trend/trend/highlights/reconcile); ?project=all and component ids stay 200 even with zero projects. Wired via ProjectNotFoundError from CostService._projects(). Note: the /chat-skill _CHAT_SKILL doc string lives in src/routes.py (blocked from doc-agent edits) and should mention this 404 behavior."}
