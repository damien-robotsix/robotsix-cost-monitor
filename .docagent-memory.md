{"properties": {"user_facing": true, "summary": "The code fix for the backend-filtered highlights bug (ticket 20260808T220028Z) is already applied to the source tree. The `service.py` `highlights()` method now filters traces by backend name (via agent-usage rows) before computing both `most_expensive_trace` and `most_expensive_session`, and returns `session_cost_scope` in the response. The existing tests in `test_service_cross_project.py`, `test_service_edge_cases.py`, and `test_service_single_project.py` all have the `session_cost_scope` assertions. \n\nOne remaining gap: the frontend `renderHighlights()` in `src/robotsix_cost_monitor/web/static/dashboard.js` does not yet use the `session_cost_scope` field to label the session highlight. The ticket spec says the label should be `session cost on <backend>` when scope is a backend name, and the existing unscoped label when `\"all\"`. The `Highlights` JSDoc typedef also needs the `session_cost_scope` field added. This file is under `src/` which the doc agent cannot modify, so the implementing developer must apply this frontend change.", "updated_memory": "Repo doc layout: README.md (API table with all endpoints and query params), AGENT.md (testing/config conventions), ARCHITECTURE.md, CONTRIBUTING.md, CHANGELOG.md (Keep-a-Changelog, unreleased section at top), docs/ MkDocs site (index.md, api.md auto-generated via mkdocstrings, reference/cli.md, reference/configuration.md, modules.yaml taxonomy). No changelog.d/ fragments directory. API docs are auto-generated from Python docstrings in src/ via mkdocstrings. \n\nHighlights endpoint fix (ticket 20260808T220028Z): The service.py `highlights()` method now (a) filters traces by backend name from agent-usage rows when backend!=all, (b) returns `session_cost_scope` field. Tests cover trace hard-filtering, per-backend session cost attribution, session_cost_scope for filtered/unfiltered, and no-match edge case. \n\nPending frontend change: `dashboard.js` `renderHighlights()` needs to read `h.session_cost_scope` and render the label as `session cost on <backend>` when scope is a backend name. The `Highlights` JSDoc typedef needs `session_cost_scope` field. File is under src/ (blocked for doc agent).\n\nreadyz route (ticket PR #484): Added `GET /readyz` readiness probe handler at `routes.py:208-211` returning `{\"status\": \"ok\"}`, mirroring the existing `/health` handler. The `_CHAT_SKILL` docstring already documented `/readyz` in the Health endpoints table (line 277 area). No other doc changes needed."}